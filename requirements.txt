# LLM providers — set ONE key in .env, the rest are optional
anthropic          # Claude (paid) — best tool-use quality
openai             # Used for Groq + Gemini (free) via OpenAI-compatible API

# Core
python-dotenv
pyyaml
requests

# Integrations (all optional)
slack-sdk
google-auth
google-auth-oauthlib
google-api-python-client

# MCP server (for Claude Desktop)
mcp[cli]>=2.0,<3.0

# Dev
pytest

# Packaging (Windows .exe build only — not needed to run the app)
pyinstaller
pillow

cryptography

# Used by routine_lock.py to detect and clear stale locks left behind by
# crashed/killed processes (e.g. Claude Desktop restarting the MCP server)
psutil
